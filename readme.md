# BookWeaver AI Backend

**BookWeaver Backend** — это "мозг" платформы BookWeaver. Это высокопроизводительный REST API сервер, который берет на себя все ресурсоемкие задачи по ML-обработке текста и генерации аудио.

## 🧠 Возможности Бэкенда

* **Оркестрация Пайплайнов:** Управляет сложной цепочкой обработки: `Парсинг -> Анализ Персонажей -> Генерация Сценария -> Синтез -> Сведение`.
* **LLM Интеграция:** Использует Google Gemini для семантического анализа текста, выделения ролей и режиссуры сцены.
* **Voice Cloning & TTS:** Генерирует речь для десятков уникальных персонажей, используя всего 3-секундные сэмплы (Zero-shot cloning).
* **Эмоциональный движок (VC):** Применяет Voice Conversion для изменения интонации (шепот, крик, плач) поверх сгенерированного голоса.
* **Smart Stitching:** Умная склейка сотен реплик в единый аудиопоток главы для бесшовного воспроизведения.
* **Backend-for-Frontend (BFF):** Специализированный API слой для оптимизации трафика мобильного клиента.

---

## 🛠 Требования

* **OS:** Linux (рекомендуется) или Windows 10/11
* **Python:** 3.10 или выше
* **GPU:** NVIDIA карта с поддержкой CUDA (настоятельно рекомендуется для TTS/VC). На CPU генерация будет работать, но медленно.
* **FFmpeg:** Должен быть установлен в системе для работы с аудио.

---

## 🚀 Установка и Запуск

### 1. Клонирование и подготовка
```
git clone https://github.com/lapcevichme/bookweaverbackend
cd backend
```
### 2. Виртуальное окружение
```
# Linux / macOS
python -m venv venv
source venv/bin/activate
```
```
# Windows
python -m venv venv
venv\Scripts\activate
```
### 3. Установка зависимостей
```
pip install --upgrade pip
pip install -r requirements.txt
```
*Примечание: Если у вас возникли проблемы с установкой `coqui-tts` или `torch`, обратитесь к [официальной документации PyTorch](https://pytorch.org/get-started/locally/) для установки версии, совместимой с вашей CUDA.*

### 4. Настройка окружения (.env)
Создайте файл `.env` в корне проекта и добавьте API ключ Gemini:
```
# .env
GEMINI_API_KEY=ваш_ключ_от_google_ai_studio
FAST_MODEL_NAME=gemini-2.0-flash
POWERFUL_MODEL_NAME=gemini-2.5-flash
# Опционально: Порт сервера (по умолчанию 8080)
SERVER_PORT=8080
```
### 5. Запуск сервера

python api_server.py

После успешного запуска вы увидите лог:
`INFO:     Uvicorn running on http://0.0.0.0:8080`

---

## 📚 Документация API

BookWeaver использует Swagger UI для автодокументации.
После запуска сервера откройте в браузере:

👉 **http://localhost:8080/docs**

Там можно протестировать все эндпоинты, включая загрузку книг (`POST /projects/import`) и управление генерацией.

---

## 📂 Структура проекта
```
backend/
├── api/                 # REST контроллеры (FastAPI Routers)
│   ├── mobile/          # API специально для мобильного приложения
│   └── ...
├── core/                # Pydantic модели и основной контекст проекта
├── pipelines/           # Бизнес-логика обработки (TTS, Scenario, VC)
├── services/            # Обертки над ML моделями (реализуют Lazy Loading)
├── utils/               # Утилиты для работы с текстом и аудио
├── input/               # (Автосоздается) Сюда кладем голоса и референсы
│   ├── voices/          # Образцы голосов персонажей
│   └── emotion_references/ # Референсы для Voice Conversion
├── output/              # (Автосоздается) Результаты генерации
└── api_server.py        # Точка входа
```
---

## 🔧 Устранение неполадок

### Ошибка `PytorchStreamReader failed reading zip archive`
Если при запуске VC или TTS вы видите эту ошибку, значит файл модели скачался с ошибками ("битый" кэш).
**Решение:**
Запустите утилиту очистки кэша:
```
python reset_model.py
```
Затем перезапустите сервер, чтобы он скачал модель заново.

### Проблемы с кодировкой на Windows
Если в консоли "кракозябры", установите переменную окружения:
```
$env:PYTHONIOENCODING = "utf-8"
```
---

## 🤝 Contributing

1.  Сделайте Fork репозитория.
2.  Создайте ветку для фичи (`git checkout -b feature/NewPipeline`).
3.  Закоммитьте изменения.
4.  Откройте Pull Request.